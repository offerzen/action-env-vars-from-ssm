on: [push]

jobs:
  test-export-environment-variables:
    runs-on: ubuntu-latest
    name: Test
    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Run action
        uses: ./
        with:
          path: '/shared/'
